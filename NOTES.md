## Этот файл для человека

Текущее состояние:
- PRD обновлён под кинотеатральные сценарии.
- Главная, карточка микса, каталог миксов, избранное, профиль и desktop/mobile компоновка реализованы.
- Оценки сессий убраны из пользовательского сценария (оцениваются только миксы).

Открытые задачи:
1. Принять продуктовое решение: оставлять `Миксы` и `Каталог миксов` как два раздела или объединять.
2. UX-polishing карточки создания микса (валидации, подсказки, редактор пропорций).
3. UX-polishing flow создания сессии (поиск и быстрый выбор микса).
4. Дополировать поведение `Смотреть всё` и пустые состояния для всех типов рейлов.
5. Расширить базу табаков/миксов из официальных источников брендов.
6. E2E smoke-checklist для desktop + mobile web.
7. Решение по финальной модели публикации миксов (публичные сразу или приватные до публикации).
