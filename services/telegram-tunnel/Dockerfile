# SSH-туннель до api.telegram.org через хост вне РФ (см. docker-compose.prod.yml,
# сервис telegram-tunnel). Только клиент ssh — вся настройка в command и volume.
FROM alpine:3.20
RUN apk add --no-cache openssh-client
