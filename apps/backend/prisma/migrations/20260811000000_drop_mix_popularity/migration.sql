-- Ручное поле популярности заменено реальным спросом: количеством событий
-- «Покурить» (SmokeCtaEvent), которое считается агрегатом по связи.
-- Значения колонки проставлялись вручную и расходились с фактическим выбором
-- гостей, поэтому переносить их некуда — колонка удаляется.
ALTER TABLE "Mix" DROP COLUMN "popularity";
