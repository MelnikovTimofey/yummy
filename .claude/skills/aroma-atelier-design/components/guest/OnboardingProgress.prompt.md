Header progress for the 2-step taste onboarding (Профили → Вкусы).

```jsx
<OnboardingProgress step={2} total={2} onBack={goBack} />
```

Back always goes one step back, then out to Знакомство. Pair with the pip row + «Открыть каталог сразу» escape hatch in the bottom dock — the guest must never feel trapped in onboarding.
